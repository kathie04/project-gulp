mixin link(href, name)
  a(href= href)= name

mixin nav(classname, ...items)
  ul(class= "" + classname)
    each item in items
      li
        a(href="#")= item

header.header
  +nav('navbar', 'About', 'Store', 'Contact', 'Login')

